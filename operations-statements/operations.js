// logical operations
const a = prompt("Enter number between 1 to 9");
const b = prompt("Enter number between 1 to 9 ");

if (a > b) {
  alert(a + " is greater than " + b);
} else {
  alert(a + "is smaller than" + b);
}
// conversion
